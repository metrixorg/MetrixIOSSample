# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

# source 'https://git.tapsell.ir/m.zareimatin/podrepo.git'

target 'MetrixTestApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MetrixTestApp
  
  # pod 'MetrixSdk', '>= 1.2.2', '< 2.0'
  pod 'Metrix', '2.0.0-beta8'
  #pod 'Pushe', '1.0.16'

  target 'MetrixTestAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MetrixTestAppUITests' do
    # Pods for testing
  end

end
